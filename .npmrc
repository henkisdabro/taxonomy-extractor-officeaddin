# Optimize for faster CI/CD builds
fund=false
audit=false
progress=false
# Use production-only dependencies during deployment
production=false
# Enable parallel installations
maxsockets=15
# Reduce network timeouts
fetch-timeout=300000
fetch-retries=3