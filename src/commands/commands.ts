/*
 * Commands file for IPG Taxonomy Extractor
 * Handles ribbon button commands and task pane launching
 */

// Function called by the ribbon button to show the task pane
function showTaskpane(): void {
  // This function is called when the ribbon button is clicked
  // The task pane will automatically be shown based on the manifest configuration
}

// Office.js initialization for commands
Office.onReady(() => {
  // Commands are ready
});